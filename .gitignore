# PowerShell / editor noise
*.swp
*.bak
*.tmp
*.log
*.original.md

# Local-only state (cache, history)
.cprompt/
cache/
history/

# OS
Thumbs.db
.DS_Store

# IDE
.vscode/
.idea/

# Pester intermediate
TestResults.xml

# Refiner bench runs: timestamped output is local-only. Commit a stable
# baseline as `bench-results/baseline.json` (or any non-prefixed name) so
# the regression test in Refiner.Quality.Tests.ps1 has something to
# compare against.
bench-results/bench-*.json
