---
description: Distill the user prompt through TRANSLaiTOR (c.ps1 local refiner) and send the resulting <task>/<context>/<constraints> XML to Claude instead of the raw text.
argument-hint: [prompt text]
---

!`c.cmd -NoRefine -Raw $ARGUMENTS`
